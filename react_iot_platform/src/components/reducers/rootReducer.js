const initState = {
    posts:[
        {id:1,name:'shreyas'},
        {id:2,name:'shemin'},
        {id:3,name:'akshay'}
    ]
}

const rootReducer = (state = initState,action)=>{
    return state;
}

export default rootReducer;